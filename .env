# 百度地图 JSAPI ak（浏览器端公开密钥）——examples 与 test 两个 demo 应用共用的唯一来源。
# 二者的 vite 配置都把 envDir 指向仓库根目录，因此只需在这里维护一份。
#
# 本地覆盖：复制为 .env.local（已被 .gitignore 忽略）填自己的 ak。
# 注意：JSAPI ak 是客户端密钥，部署后必然暴露在浏览器里，安全性靠百度控制台的
# Referer/域名白名单保证，请务必为该 ak 配好白名单。
VITE_BMAP_AK=mbKnRu5DQqM420lpbt7tbtm7WK6jiQln
