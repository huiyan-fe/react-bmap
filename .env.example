# 百度地图 JSAPI ak（浏览器端公开密钥）
#
# 仓库根目录的 .env 已内置一份共享 demo ak（examples/test 部署到官方 demo 站需用同一个
# 已配好 Referer 白名单的 ak，故 .env 随仓库提交）。本文件仅作变量说明。
#
# 本地覆盖：复制为 .env.local（已被 .gitignore 忽略），填入你自己的 ak 即可覆盖 .env。
#
# 注意：JSAPI ak 是客户端密钥，部署后必然暴露在浏览器里，安全性靠百度控制台的
# Referer/域名白名单来保证，请务必为该 ak 配置好白名单。
VITE_BMAP_AK=
